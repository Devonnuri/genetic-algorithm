{
  "name": "genetic-algorithm",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "test": "NODE_ENV=development nodemon --watch src -e ts,tsx --exec ts-node ./test/index.ts",
    "build": "babel src/ --out-dir build/"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.7.5",
    "typescript": "^3.6.3"
  },
  "dependencies": {}
}
